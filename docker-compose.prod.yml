# Override para producción: PostgreSQL solo accesible desde localhost.
# Uso en el servidor: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d postgres

services:
  postgres:
    ports:
      - '127.0.0.1:5432:5432'
